name: pdf_processing_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - ipkernel
  - pip
  - pip:
      - pymupdf  # PyMuPDF
      - pdfplumber
      - pypdf2
      - pdfminer.six